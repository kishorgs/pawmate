# PawMate Backend — never commit real secrets
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Firebase Admin service account JSON (single-line string)
FIREBASE_SERVICE_ACCOUNT_JSON=
FIREBASE_PROJECT_ID=
